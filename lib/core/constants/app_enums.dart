enum offer_status {
  pending,
  accepted,
  rejected
}
enum order_status	{
  pending,
  shipped,
  completed,
  cancelled
}
enum product_availability {
  sell,
  rent,
  both
}
enum product_status {
  active,
  sold,
  rented,
  inactive,
}
enum sos_status {
  matched,
  open,
  closed
}
enum PurchaseType {
  buy,
  rent,
}
enum ProductActionBarMode {
  owner,
  visitor,
}